{
  "name": "python3-deps",
  "buildsystem": "simple",
  "build-commands": [
    "pip3 install --no-index --find-links=\"file://${PWD}/wheels\" --prefix=${FLATPAK_DEST} -r ${PWD}/requirements.txt"
  ],
  "sources": [
    {"type": "dir", "path": "wheels", "dest": "wheels"},
    {"type": "file","path": "requirements.txt"}
  ]
}